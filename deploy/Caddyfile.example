# Copy lên EC2: sudo cp deploy/Caddyfile.example /etc/caddy/Caddyfile
# Thay YOUR-DOMAIN.com bằng domain hoặc bỏ qua nếu chỉ dùng IP (xem DEPLOY-EC2.md)

YOUR-DOMAIN.com {
  reverse_proxy localhost:4001
}
