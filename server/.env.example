PORT=4001
CORS_ORIGIN=*
# URL public người chơi mở trình duyệt (link mời). Production một cổng: trùng domain server.
CLIENT_ORIGIN=http://localhost:4001
# Tuỳ chọn: thư mục build client (mặc định ../client/dist)
# CLIENT_DIST=/app/client/dist
